class Tank {
  constructor(dx, dy, width, height, context) {
    this.dx = dx;
    this.dy = dy;
    this.width = width;
    this.height = height;
    this.context = context;
  }
  draw() {

  }
  update(dx, dy) {
    
  }
}

export default Tank;